# RFQ email delivery (Vercel serverless — recommended)
SMTP_USER=jain_engineeringworks@yahoo.co.in
SMTP_PASS=your-yahoo-app-password
SMTP_HOST=smtp.mail.yahoo.com
SMTP_PORT=465
SMTP_SECURE=true
RFQ_TO_EMAIL=jain_engineeringworks@yahoo.co.in

# Optional fallback if SMTP is not configured (client-side)
VITE_WEB3FORMS_ACCESS_KEY=
